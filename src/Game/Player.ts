export default class Player{

    score:Number;
    bot:Boolean;

    result:string;

    constructor(bot : Boolean)
    {
        this.score = 0;
        this.bot = bot;
    }

    play()
    {
        if(this.bot)
        {
           // result = 
        }
    }
}